name: CD

on:
  push:
    branches:
      - 'master'

jobs:
  cd:
    name: Continue Deployment
    uses: ./.github/workflows/release-tag.yml
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
